# 지원금AI (Auto_Gov_Macting) — 작업 규칙

## 0. 작업 순서 — 5D

```
D1 정의(Define) → D2 분할(Divide) → D3 개발(Develop)
                                        ⇅
                   D5 배포(Deliver) ← D4 진단(Diagnose)

D3과 D4는 조각마다 왕복한다. 할 일 목록이 빌 때까지 반복한다.
```

사용자가 지시할 때 **왜·무엇을·어디까지·무엇이 되면 완료인가** 중 빠진 것이 있으면 `복창` 단계에서 되물어 채운 뒤 진행한다. 특히 **"무엇이 되면 완료인가"**가 없으면 반드시 묻는다.

**한꺼번에 시키지 않는다.** 한 단계를 95% 확률로 제대로 해도 열 단계를 묶으면 최종 성공률은 약 60%로 떨어진다. 조각 하나씩 진행한다.

## 1. 지시 키워드 — 두 글자로 알아들을 것

| 단계 | 키워드 | 해석 |
|---|---|---|
| **D1** 정의 | **질문** | 사용자가 무엇을 정해야 하는지 모를 때, 하나씩 물어 채운 뒤 기획 문서로 정리한다. 혼자 지어내지 않는다 |
| | **조건** | 코드 외에 사용자가 직접 해야 할 일을 ① 한도 ② 비용 ③ 인증(며칠 걸리나) ④ 버전 네 가지로 알려준다 |
| **D2** 분할 | **쪼개기** | 되돌릴 수 있는 단위로 나누고 순서를 매긴다. 각 줄은 "이것만 되돌리면 원래대로 돌아오는" 크기여야 한다. 무엇이 무엇의 바탕인지도 표시한다 |
| | **중복** | 비슷한 기능·파일이 이미 있는지 먼저 찾는다(→ 4절 「목록」). 있으면 고치는 편이 나은지 판단해 알려준다. 새로 만드는 것은 마지막 선택지다 |
| **D3** 개발 | **복창** | 요청을 이해한 대로 먼저 말한다. **왜 원하는지(의도)**까지 포함한다. 승인 전에는 코드를 만들지 않고, 승인 후에도 한 단위씩 진행한다 |
| | **읽기** | 고치기 전에 관련 파일을 실제로 읽고, 무엇을 확인했는지 먼저 알려준 뒤 수정한다 |
| | **파급** | 함께 바뀌어야 할 것을 모두 찾아 알려준다(→ 4절 「함께 바뀌는 것」). 안 바꾸면 뭐가 깨지는지도 말한다 |
| **D4** 진단 | **실행** | 코드를 읽은 것인지 **실제로 실행한 것인지 구분해** 보고한다. 실행했으면 결과(출력·화면·숫자)를 보여준다. 정상 경로뿐 아니라 취소·중단·잘못된 입력도 시험했는지 밝힌다 |
| | **추측** | 원인이 추정인지 확인된 사실인지 **구분해 말한다.** 확인 안 했으면 **가릴 수 있는 측정 방법을 먼저 만든다.** 같은 문제에서 진단이 두 번 빗나가면 세 번째 추정 대신 측정 도구를 만든다 |
| | **확정** | 이번에 뭐가 안 됐는지 짚고 **규칙 문장으로 만든다.** 세 곳에 반영 — ① 6절(규칙) ② 검사(재발하면 자동으로 걸리게) ③ 4절(목록) |
| **D5** 배포 | **감지** | 실패했을 때 사용자가 알아챌 방법을 만든다. 처리 건수를 기록하고 **0건일 때도 보고**하며, 실패 시 알림 경로를 정한다 |
| | **보안** | 배포 전 다섯 가지를 확인해 보고한다 — ① 인증 키가 프론트엔드 코드에 있는가 ② 로그인 없이 남의 자료가 조회되는가 ③ 권한을 서버에서 확인하는가 ④ 관리자 화면이 공개돼 있는가 ⑤ 키가 저장소 이력에 올라간 적 있는가. **화면에서만 막은 것은 막은 것이 아니다** |

### AI에게 맡기지 않을 것

**판단 기준**: *"AI가 스스로 맞았는지 확인할 수 있는 일인가?"* 확인할 수 있으면 맡기고, 없으면 사용자가 정한다.

| 맡긴다 | 사용자가 정한다 |
|---|---|
| 코드 작성 · 형식 변환 · 계산 · 반복 작업 | 무엇을 만들 것인가 |
| | **이 프로젝트만의 업무 규칙**(→ 6절) |
| | 어디까지 하고 무엇을 안 할 것인가 |
| | 화면이 보기 좋은가 |

**정해주지 않은 규칙을 AI가 채우면 그럴듯한 것을 지어낸다.** 실제로 매칭 철학을 정해두지 않아 4월에 통째로 뒤집은 이력이 있다.

## 2. 항상 지킬 것

### 2-1. 확인 없이 진행하지 않는다
새 작업·범위 변경·되돌리기 어려운 작업(삭제·배포·외부 발송)은 이해한 내용과 **완료 기준**을 먼저 확인받는다. 승인 전에는 코드를 만들지 않는다.
**건너뛰는 경우**: 한 문장으로 설명되는 작업(오타·문구·색상·값 하나).

### 2-2. 읽지 않고 고치지 않는다
기존 파일 수정 시 반드시 먼저 읽는다. 내용을 짐작해서 수정하지 않는다.

### 2-3. 검증 보고는 실제로 한 것만 (절대 원칙)
- **"코드상 맞다"와 "실행해서 됐다"를 구분해 보고한다**
- 하지 못한 검증은 왜 못 했는지 밝힌다
- **"정상 동작할 것입니다"와 "정상 동작을 확인했습니다"는 완전히 다르다**
- ❌ *"5가지 페르소나로 검증했습니다"* (실제로는 문자열 조립만 확인)
- ✅ *"코드 경로상 전달되는 것은 확인했습니다. 실제 응답 반영 여부는 API 호출이 불가하여 미확인입니다."*

### 2-3-1. 답변 첫 줄에 「어디를 봤는지」를 적는다 (형식)

**답하기 전에 이 줄부터 쓴다.**

```
[확인] 3/3
 · verify table knowledge_base   → 읽는 곳 26곳
 · 그 쿼리 직접 실행              → 고아 76% 섞임
 · 프로덕션 상담 실행             → 정상
```

| 표시 | 뜻 |
|---|---|
| `[확인] N/N` | 아래 목록의 「봐야 할 곳」을 다 봤다 |
| `[부족] N/M` | 덜 봤다. **안 본 것에 ★를 붙여 적는다** |
| `[미확인]` | 확인하지 않고 답한다 — 그렇게 밝힌다 |
| `[판단]` | 사실 주장이 아니라 설계·제안·의견이다 |

### 질문 유형별 「봐야 할 곳」

전부 2026-08-25 에 실제로 틀린 사례에서 뽑았다. 도구는 `backend/scripts/verify.py`(→ 8절).

| 이런 질문·주장 | ① | ② | ③ |
|---|---|---|---|
| 「그런 기능·함수·API 가 있나/없나」 | 전체 검색 | **다른 이름으로 한 번 더** | |
| 「이 데이터 안 쓰인다 / 지워도 되나」 | 읽는 코드 찾기 | **그 쿼리를 그대로 실행** | 서비스 한 번 돌리기 |
| 「이 주소·요청이 되나/안 되나」 | 그대로 요청 | **파라미터 조합 바꿔서** | |
| 「이 설정이 켜져 있나」 | 로컬 | **프로덕션** | |
| 「이게 고장났나 / 죽었나」 | 표본 1회 | **표본 2회 이상** | 다른 환경·회선에서 |
| 「화면이 이렇게 나온다」 | 캡처 1장 | **재현 1회 더** | |
| 「고쳤다 / 된다」 | 코드 확인 | **실제 실행 + 출력 붙이기** | |

**②열이 오늘 전부 빠졌던 자리다.** ①만 하고 다 봤다고 느꼈다.

### 왜 이 형식인가 — 규칙이 아니라 형식인 이유

전역 CLAUDE.md 「🔴 AI 응답 절대 원칙 · 추측 금지」는 **우선순위 2위 · 예외 없음**으로
이미 지정돼 있다. 그런데도 2026-08-25 하루에 일곱 번 어겼다. 같은 날 지켜진 지침도 있다 —
한국어로 답하기, 커밋에 `Co-Authored-By` 넣기, 읽기 전에 고치지 않기. 차이는 하나다.

```
지켜진 것    언제 할지가 정해져 있다. 판단이 필요 없다
어긴 것      "확인했는가" 를 스스로 판단해야 한다 ← 그 판단이 틀린다
```

**「확인한 것만 말한다」는 판단이라 실패했다. 「첫 줄에 N/M 을 쓴다」는 형식이라 빈칸이 보인다.**
안 본 것이 있으면 ★가 남고, 대표는 「그거 왜 안 했어?」 한마디만 하면 된다.

### 측정 — 이 방법이 듣는지 세어 본다

기준선(2026-08-25 하루):

```
대표가 되물어 드러난 것   3건   semas 링크 · classification_events · /api/match
나중에 스스로 정정한 것   4건   url_health · 결제위젯 · 임베딩 설정 · gbsa 오탐
합계                    7건
```

앞으로 셀 것은 하나다 — **`[확인]`이라 써 놓고 틀린 횟수.**
대표가 「틀렸다」고 하면 그 자리에서 `C:\DevLogs\Auto_Gov_Macting\01_errors.md` 에 한 줄 남긴다.

> `2026-08-26  [확인] 3/3 이라 했으나 프로덕션 미확인 — 대표 지적`

일주일 뒤 줄 수를 센다. **줄지 않으면 이 방법도 틀린 것이니 훅으로 기계 강제를 얹는다.**
효과가 확인되기 전에는 전역 CLAUDE.md 로 올리지 않는다 — 안 지켜지는 규칙을 하나 더
늘리는 꼴이 된다.

### 그 밖

**임계값·기준값은 재서 정한다.** 「200이 아니면 죽음」처럼 그럴듯한 규칙을 세우지 않는다.
실제로 재본 결과 죽은 K-Startup 링크도 HTTP 200에 본문 2,635자를 돌려줬다. 재고 나서야
「저장된 제목이 페이지에 있는가」가 유일하게 통한다는 것을 알았다.
**재서 알아낸 값은 그 실측 케이스를 테스트로 못박는다.** 안 그러면 다음 사람이 되돌린다.

**이름·함수가 있다고 가정하는 것**(`_uuid`·`_safe_exec`·`_check_profile_match`)은
사람이 아니라 **ruff F821 커밋 훅**이 막는다(→ 8절). 기계가 잡는 유일한 갈래다.

### 2-4. 한 번에 하나만 바꾼다
여러 곳을 동시에 바꾸면 원인을 알 수 없다. 되돌릴 방법을 확인한 뒤 하나씩 바꾼다.

### 2-5. 끝나면 잔재를 지운다
임시 파일·테스트 스크립트·시험용 데이터는 작업이 끝나면 정리한다. 남길 것은 왜 남기는지 말한다.

### 2-6. 요청하지 않은 것은 만들지 않는다
범위 밖 기능, 나중을 위한 유연성, 일어나지 않을 상황 처리는 넣지 않는다.

## 3. 기록

`C:\DevLogs\Auto_Gov_Macting\` 에 남긴다.
- 오류 해결 → `01_errors.md` (증상·원인·해결·교훈)
- 기능 개선 → `02_improvements.md`
- 설계 결정 → `03_decisions.md` (결정·이유·대안)

**성공한 것도 기록한다.** 무엇이 효과가 있었는지 되짚으려면 성공 기록이 필요하다.

## 4. 목록 — 지금 이 프로젝트에 있는 것

> 작업 시작 전 이 절을 먼저 본다. 새로 만들기 전에 여기 있는지 확인한다.

### 무엇을 하는 프로그램인가
정부·지자체 지원사업 공고를 자동 수집·분석해 기업과 개인에게 맞춤 매칭하고, AI 상담과 신청 도움을 제공하는 서비스 (govmatch.kr)

### 구조
| 구분 | 위치 | 설명 |
|---|---|---|
| 서버(주방) | `backend/app/` | FastAPI. `main.py`가 API 진입점 |
| 화면(홀) | `frontend/src/` | Next.js App Router |
| 저장소(창고) | Supabase PostgreSQL | PgBouncer 경유(포트 6543) |

### backend/app/services 하위
| 폴더 | 하는 일 |
|---|---|
| `scrapers/` | 공고 수집 (39개 파일) |
| `patrol/` | 일일 파이프라인·재분석·모니터링 |
| `orchestrator/` | AI COO — 운영 보고·감시 |
| `consulting/` | AI 상담 |
| `ai_engine/` | AI 호출 공통 |
| `prompts/` | 프롬프트 모음 |
| `financial_analysis/` | 재무 분석 |

### 수집처를 추가할 때
```
위치: backend/app/services/scrapers/tier1/
공통: scrapers/base.py 의 run() 을 상속해 쓴다
```

**⚠️ `agency_scrapersN.py` 형태로 새 파일을 만들지 말 것.** 현재 `agency_scrapers3.py` ~ `agency_scrapers13.py` **11개**가 이 방식으로 늘어난 상태다. 새 수집처는 기존 파일에 추가하거나, 기관명을 딴 파일(`ksa_scraper.py` 같은)로 만든다.

### 함께 바뀌는 것 (실측 — 하나 고치면 다른 쪽도 봐야 한다)

| 함께 바뀐 횟수 | 파일 쌍 |
|---:|---|
| 48회 | `backend/app/main.py` ⇄ `services/ai_consultant.py` |
| **47회** | **`backend/app/main.py` ⇄ `frontend/src/components/Dashboard.tsx`** |
| 32회 | `main.py` ⇄ `components/pro/ProSecretary.tsx` |
| 29회 | `Dashboard.tsx` ⇄ `ResultCard.tsx` |
| 23회 | `core/matcher.py` ⇄ `main.py` |

**서버 응답 형태를 바꾸면 화면도 함께 고쳐야 한다.** 계약이 코드에 명시돼 있지 않으므로 반드시 확인한다.

### 큰 파일 (수정 시 주의)
```
main.py            17,153줄   ← 가장 많이 고쳐진 파일(585회). 여기에 더 붙이기 전에 분리를 검토
ai_consultant.py    3,643줄
ProSecretary.tsx    3,013줄
Dashboard.tsx       2,609줄
```

## 5. 외부 서비스

| 서비스 | 용도 | 한도·과금 | 확인처 |
|---|---|---|---|
| **Supabase** | DB | **Micro 플랜**(Nano에서 업그레이드). Nano 시절 **Disk IO 예산 30분/일 소진으로 사이트 전체 다운** 이력 있음 | Supabase 대시보드 Disk IO 차트 |
| **Gemini API** | 공고 분석·상담·블로그 | **선불 크레딧.** 소진되면 429로 조용히 전부 실패. **7월 요금 305,690원**(출력 83%) | Google AI Studio 결제 |
| **OpenAI** | 상담 폴백 | 선불. 과거 Gemini와 **동시 소진**되어 상담 전면 중단 이력 | OpenAI 대시보드 |
| **PortOne V2** | 결제 | **카카오페이만 작동.** 카드(V1)는 `PORTONE_V1_API_KEY` 미설정으로 미작동 | PortOne 콘솔 |
| **Railway** | 백엔드 배포 | `backend/railway.toml` (Dockerfile 빌드, healthcheck `/health`) | Railway 대시보드(CLI 미설치 — 웹으로 관리) |
| **Vercel** | 프론트 배포 | git push 연동 | Vercel 대시보드 |
| 공공 API | 공고 수집 | `PUBLIC_DATA_PORTAL_KEY`, `BIZINFO_PORTAL_KEY`, GOV24 등 | data.go.kr |
| OAuth | 소셜 로그인 | 카카오·네이버·구글. **콘솔 등록·심사 필요** | 각 개발자 콘솔 |

**⚠️ AI 크레딧과 DB 한도는 바닥나면 조용히 멈춘다.** 대량 작업 전 잔액을 확인한다.

## 6. 이 프로젝트만의 규칙

> 머릿속에만 있으면 AI는 모른다. 확인된 것만 적는다.

### 매칭
- **정부사업 매칭은 제외 규칙(exclusion_rules) 기반이다.** 우대·지정 조건은 영향이 미미하다
- **자격은 필터, 점수는 적합도** — 자격 미달도 완전 제외하지 않고 후순위로 민다
- 매칭 판정은 `announcements.eligibility_logic`을 쓴다. 이 값이 비면 "확인 필요"로 강등한다

### 데이터
- **DB 컬럼 삭제·이름 변경 금지. 추가만 허용한다** (되돌릴 수 없음)
- 마감일이 없는 공고를 "상시모집"으로 표시하지 않는다. **비어 있는 것과 상시는 다르다**
- 목록에 마감일 컬럼이 없는 사이트에서 **아무 날짜나 마감일로 저장하지 않는다** (등록일을 마감일로 오인해 진행중 공고가 전량 사라진 이력 있음)

### 화면·정책
- **비로그인 상태에서 공고 클릭 시 로그인을 요구하는 것은 의도된 설계다.** 제거하지 않는다
- 비로그인 화면은 별도 컴포넌트를 만들지 않고 `Dashboard`+`ResultCard`에 `isPublic` prop으로 재사용한다

### 결제
- 2026-07-16 **크레딧 통일** 방향으로 결정됨(구독 유지에서 전환). 결제 관련 변경 시 이 방향을 확인한다
- 결제 API는 **프론트·백엔드 양쪽에서 이중 검증**한다 (결제창을 닫았는데 구독이 시작된 사고 이력)

### AI 호출
- AI가 추출한 필드는 **타입이 비결정적이다**(문자열/리스트). DB 스칼라 컬럼에 넣기 전 문자열로 강제한다
- `dict.get(key, '')` 대신 `(d.get(key) or '')`를 쓴다. 키가 있고 값이 null이면 default가 무시된다
- 외부 API 호출의 예외를 좁게 잡지 않는다. `except ValueError`만 잡으면 실제 장애(500·타임아웃)가 통과한다

## 7. 되돌리기

- **저장소**: `https://github.com/iloom50-commits/govmatch.git` (origin)
- **배포**: 백엔드 Railway / 프론트 Vercel. **main 브랜치 push 시 자동 배포**
- **배포 정책**: 로컬 테스트 → git push
- **되돌리는 법**: `git revert` 후 push하면 자동 재배포
- **되돌릴 수 없는 것**: DB 컬럼 삭제·변경, 데이터 삭제, 외부 발송(메일·알림), 실결제

## 8. 검증 도구

### 확인 도구 — 짐작으로 답하지 않기 위한 것

```
cd backend && python scripts/verify.py <명령> <대상>
```

**말하기 전에 돌린다.** 아래 표의 왼쪽 같은 말이 나오려 하면, 오른쪽 명령을 먼저 실행한다.
출력에 없는 것은 **「미확인」**이라고 쓴다. 그럴듯한 추정으로 채우지 않는다.

| 이런 말이 나오려 하면 | 이걸 먼저 돌린다 |
|---|---|
| 「그런 기능은 없다 / 새로 만들어야 한다」 | `grep <기능어>` |
| 「이 함수·변수는 있다 / 쓸 수 있다」 | `symbol <이름>` |
| 「그런 API 는 없다 / 이 주소로 부르면 된다」 | `endpoint /api/...` |
| 「이 데이터는 안 쓰인다 / 지워도 된다」 | `table <테이블>` |
| 「이 주소는 안 열린다 / POST 여야 한다」 | `url "<주소>"` |
| 「이 수집처 링크가 죽었다」 | `live <호스트>` |
| 「이 설정은 켜져 있다 / 꺼져 있다」 | `env <이름>` |

**왜 만들었나 (2026-08-25).** 하루 동안 확인 없이 단정한 것이 일곱 건 나왔다.
전부 30초면 확인되는 것이었다 — 「링크 검사 장치가 없다」(있었다), 「POST 전용이라
고유 주소가 없다」(파라미터 하나가 빠졌을 뿐이었다), 「지우면 통계가 달라진다」(숫자가
그대로였다). **2-3-1 규칙을 써 둔 다음 날에도 세 번 어겼다. 규칙만으로는 안 된다.**
ruff 훅이 작동한 이유는 기계가 강제하고 0.5초에 끝나기 때문이다. 이 도구도 같은 노림수다 —
**확인 비용을 0으로 만들어 「안 할 이유」를 없앤다.**

⚠️ **`table` 의 「읽는 곳」은 출발점이지 결론이 아니다.** 고아·불필요 데이터가 실제
결과에 섞이는지는 **그 쿼리를 그대로 실행해** 봐야 안다. `WHERE` 에 조건이 없으면 섞인다.
실제로 `knowledge_base` 는 상담 RAG 쿼리에 `announcement_id` 조건이 없어 고아 76%가
후보에 들어가고 있었다 — 쿼리를 돌려 보기 전에는 몰랐다.

### 커밋 전 정적 검사 (설치 필요)

```
git config core.hooksPath .githooks     ← 클론할 때마다 한 번
pip install ruff
```

`.githooks/pre-commit` 이 백엔드 파이썬의 **정의되지 않은 이름(F821)** 을 0.5초에 잡는다.
설정은 `backend/ruff.toml`. 수동 실행은 `cd backend && python -m ruff check app/`.

**이 검사가 없어서 실제로 벌어진 일**(2026-08-24 실측):

| 잠복 | 결함 | 결과 |
|---|---|---|
| 3개월 | `_seed_keyword_synonyms()` 의 `_safe_exec` | `init_database()` 가 604행에서 중단 → **`mail_signals` 테이블 누락** |
| 4개월 | 정의 없는 `_check_profile_match` 호출 | 상담 2차 필터 경로 NameError |
| — | `_fetch_ga4_data()` 의 `requests` | GA4 지표 조회 불가 |
| 하루 | 결제 주문 API 의 `_uuid` | `/api/toss/order` 500 |

**`init_database()` 는 최상위 `except` 가 실패를 `print` 만 하고 삼킨다.** 그래서 3개월간
아무도 몰랐다. 여기에 테이블을 추가할 때는 배포 후 **그 테이블이 실제로 생겼는지 DB에서 확인한다.**

F401(미사용 import) 62건은 일부러 켜지 않았다. **통과할 수 없는 검사는 검사가 아니다** —
차단하면 `--no-verify` 로 우회하게 된다.

### 그 밖

- 화면 검증용 공용 마스터 계정 `master@govmatch.kr` 가 있다 (코드에 하드코딩된 것은 아니며 DB 계정)
- 결제 실검증용 소액 오버라이드 `SUBSCRIPTION_TEST_PRICE` 가 `main.py:2878` 에 있다. **기본 OFF이며 테스트 후 반드시 원복한다**
- 화면 검증은 Playwright(`frontend/tests/e2e/`)로 한다. **캡처를 남겨 눈으로 확인한다**
